# 2026-09-21

## MetalWatch（飞牛 FPK 硬件服务器监控）项目启动

工作区 `D:\pj\MetalWatch` 此前为空，本次从零建立骨架 + 设计文档。

### 完成内容

1. **核实飞牛 FPK 官方规范**（developer.fnnas.com，通过 ckcoding/fnnas-docs 的 ALL_DOCS.md 单文件版摘原文）：
   - 包结构：`manifest`（INI，无扩展名）/ `ICON.PNG` + `ICON_256.PNG` / `config/privilege` + `config/resource`（JSON）/
     `cmd/`（main + 8 个生命周期钩子）/ `wizard/{install,config,uninstall,upgrade}`（JSON 数组）/ `app/`（payload，Docker 型放 `app/docker/docker-compose.yaml`）/ `app/ui/config`（桌面入口）
   - 打包强校验：manifest、config/privilege、config/resource、ICON.PNG、ICON_256.PNG 必须存在
   - 环境变量：`TRIM_APPDEST / TRIM_PKGETC / TRIM_PKGVAR / TRIM_PKGTMP / TRIM_PKGHOME / TRIM_PKGMETA / TRIM_APPDEST_VOL / TRIM_SERVICE_PORT / TRIM_DATA_SHARE_PATHS / TRIM_UID / TRIM_GID / TRIM_TEMP_LOGFILE`
   - `cmd/main` 退出码语义：0=运行中、3=未运行、1=失败（错则应用面板状态错）
   - wizard 字段名即环境变量名，自定义字段建议 `wizard_` 前缀
   - **关键约束**：manifest 只声明一个 `service_port`，且必须与 compose 端口映射、`app/ui/config` 的 port 三处一致

2. **产出交付物**：
   - `deploy/fpk/metalwatch/` 可 `fnpack build` 的完整骨架（manifest/privilege/resource/compose/prometheus.yml/5 个 cmd 脚本/3 个 wizard/ui config/真实 PNG 图标）
   - `deploy/tools/gen_icons.py` 纯标准库生成 64/256 图标（可重跑）
   - `deploy/tools/check_fpk.py` 骨架校验：JSON 合法性、cmd 脚本 LF 换行、端口三处一致性、向导字段与 compose 引用一致性
   - `docs/01-架构评审与关键决策.md`（D1–D12）
   - `docs/02-模块需求拆解与验收.md`（M1–M9 + 验收标准 + 里程碑映射）
   - `docs/03-数据库设计.md`（MySQL 建表 DDL + Prometheus 指标模型 + 容量测算）
   - `docs/04-接口契约.md`（Agent 上报 / 管理 API / Webhook / 鉴权与错误码）
   - `README.md`

### 关键架构决策（供后续会话直接沿用）

- **D2**：飞牛官方文档**没有**第三方 FPK 向「日志中心」投递日志的能力 → 降级为应用内 jsonl 日志 + 日志检索页；待真机验证。
- **D3**：单端口分流，Agent 上报与 WebUI 共用 `service_port`（当前 18080 → 容器 8080）。
- **D4**：官方未提供 FPK 读取 fnOS 用户/角色的能力 → 应用自建 RBAC（admin/operator/viewer）。
- **D1**：持久化一律走 `TRIM_PKGVAR`；卸载默认保留 + 先 mysqldump 到共享目录。
- **D9**：BMC 密码 AES-256-GCM，AAD 绑定 host_id，主密钥独立生成于 `TRIM_PKGVAR/data/secret/master.key`（0600）。
- **D12**：Prometheus 容量 ≈60MB/机/180天（单机约 60 条时间线、30s 采样）；建议 raw 30 天 + 5m 聚合长保留，而非单参数保留期。
- 告警去重：`alert_event.active_key` 唯一索引，firing 写去重键 / resolved 置 NULL（MySQL 无部分唯一索引的绕过法）。

### 待真机验证（V1–V7）

compose 中 `${TRIM_UID}:${TRIM_GID}` 与 `${wizard_*}` 插值是否生效；非 root 容器对 TRIM_PKGVAR 子目录写权限；卸载保留后重装数据是否恢复；日志中心可行性；service_port 能否安装后改写；ARM 多架构镜像；8GB 机型资源实测。

### 环境备注

本机 Git Bash 的 `mkdir` / `tail` / `head` / `dirname` / `ls` 均不可用 → 一律用托管 Python 执行文件操作与校验；核对仓库状态用 `git -C <abs> <cmd>`，不要 `cd && git`。

## 下午：纳入 git 管理

- 先做文件系统级备份到 `D:\pj\.git-backups\MetalWatch-20260921-222630`（28 文件），再动 git。
- **提交身份**：用户指定 `YiDaZhang`；邮箱按 fn-finstat 习惯配为 `YiDaZhang@noreply.gitee.com`（仅本仓库 local config，**待用户确认是否为 Gitee 注册邮箱**，否则提交无法归属到账号）。全局 git 无身份配置。
- **分支**：`main` + `dev` 双分支，HEAD 指向 `dev`。远程（Gitee）地址待用户提供。
- 提交：`4e4e3b4`（main，28 文件）+ `8949691`（dev，README 补充 Git 约定）。
- **建分支避坑**：不用 `git checkout -b`（本机曾因此误删 83 个文件），改用 `git branch dev` → `git switch dev`（同 commit 切换，工作区零改动），并用 Python 脚本 `D:\pj\.git-backups\setup_branches.py` 在切换前后做 sha256 清单比对验证：丢失=0 新增=0 变化=0。
- **行尾坑（重要）**：`.gitattributes` 里 `cmd/*` **只匹配仓库根目录**，本项目路径是 `deploy/fpk/metalwatch/cmd/`，导致规则失效并刷出 24 条 LF→CRLF 警告。正确写法 `**/cmd/*` + `**/cmd/**`，并把 `*` 设为 `text=auto eol=lf`、仓库 local 设 `core.autocrlf=false`。最终 24 个文本文件 `w/lf`、CRLF 为 0。
- `git fsck` 通过（2 个 dangling blob 是 .gitattributes/.gitignore 改版残留，无害）；`git branch -vv` 两分支引用正常。

