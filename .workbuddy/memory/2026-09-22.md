# 2026-09-22 工作日志

## 全量重设计（gRPC 双向流 + Mesh + BMC 管控）

用户下发终版设计方案，据此重整全仓库：

- **协议换血**：Agent 通道由 HTTP/2 + Protobuf 改为 **gRPC 双向流**（`AgentStreamService.Stream/Enroll`）。
- **proto 三件套**：`agent.proto` / `bmc.proto` / `mesh.proto`，已用 `D:\dev\protoc` v36.2 生成到
  `backend/proto/gen/`（包名 `mwpb`）。工具链含 `protoc-gen-go` + `protoc-gen-go-grpc`，装在 `D:\dev\gobin`。
- **引擎层落地**：`internal/engine/` 四个引擎（mesh Dijkstra / schedule / alarm / control）+ `pkg/ipmi`
  （Executor 注入式，可测）+ 单元测试。
- **前端重整**：Vite 8→5、ECharts 6→5、新增 Arco Design Vue、9 页面（新增 BMC 管控 / Mesh 拓扑）。
  Node 22.22.2 与 24.21.0 双构建 + `vue-tsc` 均通过（16 chunk）。
- **文档按类型重整**：`docs/` 由扁平编号改为 `01-架构 / 02-设计 / 03-计划 / 04-部署 / 05-记录`，
  新增总体架构、通信协议、进度看板、FPK 部署四份文档，关键决策追加 D22–D30。

### 子代理产出必须复核（本次实测）

引擎 + IPMI 由子代理产出，`go build` 通过但 **3 个测试真实失败**：

1. `parseSDR` **实现 bug**：`ipmitool sdr` 的单位与数值同在第 2 段（"42 degrees C"），
   原实现误把第 3 段（"ok"）当单位 → Unit 变 "ok"、风扇 RPM 永远匹配不到。已改为「末段为状态」。
2. `TestFanHysteresisDelta` **测试写错**：temp=35 落在曲线 `≤45→35%` 段，期望值却写 20。
   实现是对的，改的是断言（100→35 相差 65 ≥ MinDelta 50，应动作）。
3. 修复后全量 `go test ./...` 通过。

教训：子代理报告"完成"不等于正确，**必须自己跑 build + test 复核**。

### 本机环境新事实

- 直连 github.com:443 超时；git 与下载都需走代理 `http://127.0.0.1:7890`（已写入本仓库 `.git/config`）。
- 新环境一律装到 `D:\dev`（用户 2026-09-21 明确要求）。protoc 装在 `D:\dev\protoc`，Go 插件在 `D:\dev\gobin`。
- `prometheus/prometheus/tsdb` 作为库引入代价过大：`go get` 超过 12 分钟未完成 →
  内嵌时序改自研（基于已有 SQLite 的分块压缩存储），见决策 D25。

## 托管与 module 路径迁移到 GitHub（提交 dadbc30）

- 起因：用户要求去掉 `gitee.com/zhangyilin_233`，换成 GitHub 信息。远程早已是
  `https://github.com/LarryMKott/metalwatch.git`，只是 module 路径仍是 Gitee。
- 替换范围：37 个文件（两个 go.mod、全部 Go import、3 份 .proto），
  `gitee.com/zhangyilin_233` → `github.com/LarryMKott`。
- **关键坑（新踩，已写入 MEMORY.md）**：`proto/gen/*.pb.go` 里的 `rawDesc` 是**带长度前缀的
  序列化 FileDescriptorProto**，go_package 字符串长度 50→47，文本替换会破坏 options 与
  go_package 两处 varint 长度前缀 → 运行时注册失败。**必须改 .proto 后重新 protoc 生成**
  （`D:\dev\protoc\bin\protoc.exe` + `--go_out/--go-grpc_out` + `paths=source_relative`）。
- 提交邮箱：`YiDaZhang@noreply.gitee.com` → `LarryMKott@users.noreply.github.com`（local config）。
- 验证：backend `go build ./...` + `go test ./...` 全绿；agent `go build ./...` +
  linux-amd64 / linux-arm64 / windows-amd64 三平台交叉编译均 rc=0。
- 全仓库 `zhangyilin_233` / `gitee.com` 残留为 0；CRLF 仅剩两个 PNG 图标（二进制，正常）。
- 推送：用 refspec `git push origin dev:dev` + `dev:main`（**不切换分支**，规避本机 checkout
  误删文件风险），dev 与 main 均指向 dadbc30，已用 `git ls-remote` 核实。

## 顺手清理

- `.workbuddy/memory/MEMORY.md` 此前被重复写入一整个副本（211 行 → 120 行），已去重，
  Git 约定段更新为 GitHub 托管 + 代理配置 + module 路径。
- 根 README 的 docs 目录说明仍是旧扁平结构，已同步为 `01-架构/02-设计/03-计划/04-部署/05-记录`。
- 开发计划待拍板项 C4（提交邮箱是 Gitee 邮箱）标记已闭环。

## 提交 Phase 1 数据面闭环（提交 72d8afc，82 文件 / +9966 行）

工作区积压了一批完整业务实现（非本次会话所写，为子代理产出），提交前跑门禁发现
**3 个真问题，全部修掉后才提交**：

1. **`internal/notify` 包编译失败**：`AlertEventRepo.UpsertFiring` 签名已改为
   `(bool, int64, error)`（多返回 alert_id），`webhook_test.go` 仍按 2 个值接收。
   → `go vet` / `go test` 报 build failed。**教训：仓储返回值的改动必须跟着改测试**。
2. **`service/inventory.go` 把「拔了又插」误判成新增**：diff 的既有部件索引用
   `ListActive`（只含在役），认不出已被 `MarkRemoved` 的部件 → 回归被判 `added`。
   原代码还想把 change_type 写成中文「回归」，而 DDL 有
   `CHECK (change_type IN ('added','removed','modified'))`，**真走到该分支会直接违反约束**。
   → 新增 `Components().ListAll(ctx, hostID, category)`（含已移除件）建索引，
   回归判为 stay、不落事件；顺手删掉遗留的 `s.log.Warn("DEBUG lookup", ...)` 调试日志。
3. `internal/app/app.go` 未 gofmt。

其余要点：

- **`-count=1` 很关键**：第一次全量 `go test ./...` 全 ok 但多是 `(cached)`；
  强制重跑才暴露上面第 1、2 条。以后验子代理产物一律 `-count=1`。
- **偶发假失败**：多包并发跑 `internal/service` 时出现过一次
  `TempDir RemoveAll cleanup: unlinkat ... directory is not empty`，
  单独重复跑 3 次全绿 —— Windows 临时目录清理竞争，非业务缺陷；Linux CI 不受影响，但值得盯。
- 前端配置改动：WS 实时告警随 W7 交付开启（`.env.*`），vite dev proxy 补 `ws: true`；
  Node 22 下 `vue-tsc` + `vite build` 均通过（28 chunk）。
- `backend/%SystemDrive%` 是 Go 工具链在受限环境写出的垃圾目录（约 1MB），
  已被 .gitignore 忽略、未被跟踪，未清理。
- 本机**没有 `gh` CLI**，无法用命令行查 GitHub Actions 结果，需用户自行在网页确认。
