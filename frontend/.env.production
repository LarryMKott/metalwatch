# 生产环境：前端与后端同源（同一 service_port 由服务端提供静态资源与 API）
VITE_APP_TITLE=MetalWatch
VITE_API_BASE=/api/v1
VITE_WS_PATH=/api/v1/ws/alerts
# 实时告警 WS 默认关闭：后端 /api/v1/ws/alerts 由 W7 交付，未实现时连接会不断重连刷错误。
# W7 落地后改为 true 即可启用；关闭状态下顶部状态按 30s 轮询刷新。
VITE_WS_ENABLED=false
