# 全仓库统一 LF：本机是 Windows，但产物要在 Linux（飞牛 OS）上打包运行
* text=auto eol=lf

# 飞牛生命周期脚本必须是 LF —— 带 CRLF 会导致真机上执行失败
# 注意：cmd 目录在仓库里是多层深（deploy/fpk/<app>/cmd/），必须用 **/ 前缀
**/cmd/* text eol=lf
**/cmd/** text eol=lf
*.sh text eol=lf

# .bat 是 Windows 专用：cmd.exe 解析 if/for/goto 块对行尾敏感，
# 全仓库的 LF 规则在这里属于误伤。仓库内仍按 LF 存储，检出时转成 CRLF。
*.bat text eol=crlf

# 二进制
*.png binary
*.PNG binary
*.fpk binary
*.mmdb binary
*.xlsx binary
*.pdf binary
